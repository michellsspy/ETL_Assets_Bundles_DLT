# The main pipeline for etl_assets_bundles_dlt
resources:
  pipelines:
    etl_assets_bundles_dlt_pipeline:
      name: etl_assets_bundles_dlt_pipeline
      catalog: workspace
      schema: etl_assets_bundles_dlt_${bundle.target}
      serverless: true
      libraries:
        - notebook:
            path: ../src/pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
