# resources/dlt_refined.pipeline.yml
# Define o pipeline responsável pela transição: Trusted -> Refined

resources:
  pipelines:
    dlt_refined_pipeline:
      name: "DLT - 3. Pipeline Trusted para Refined (Star Schema)"
      catalog: "dev"
      target: "refined"
      serverless: true
      libraries:
        - notebook:
            path: "../src/raw/consumos/dlt.py"
      development: true
      continuous: false