# This is a Databricks asset bundle definition for etl_assets_bundles_dlt.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: etl_assets_bundles_dlt
  uuid: 6c1bf831-f9f9-4309-a316-88adb20a3647

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-03ce1db1-207c.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-03ce1db1-207c.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/michel.data.engineer@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/michel.data.engineer@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: michel.data.engineer@gmail.com
        level: CAN_MANAGE
