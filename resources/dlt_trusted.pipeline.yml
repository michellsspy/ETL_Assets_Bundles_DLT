# resources/dlt_trusted.pipeline.yml
# Define o pipeline responsável pela transição: Raw -> Trusted

resources:
  pipelines:
    dlt_trusted_pipeline:
      name: "DLT - 2. Pipeline Raw para Trusted"
      catalog: "dev"
      target: "trusted"
      serverless: true
      libraries:
        - notebook:
            path: "../src/raw/consumos/dlt.py"
      development: true
      continuous: false