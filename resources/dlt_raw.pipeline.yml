# resources/dlt_raw.pipeline.yml
# Define o pipeline responsável pela transição: Transient -> Raw

resources:
  pipelines:
    dlt_raw_pipeline:
      name: "DLT - 1. Pipeline Transient para Raw"
      catalog: "dev"
      target: "raw"
      serverless: true
      libraries:
        - file:
            path: "../src/dlt_pipelines/dlt_raw_pipeline.py"
      development: true
      continuous: false